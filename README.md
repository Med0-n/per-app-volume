# per-app-volume
🎚️ Per App Volume — GNOME Shell extension to control each application's volume directly from the panel. Features per-app sliders, mute buttons, master volume, 150% boost, liquid glass UI, scroll-to-adjust and a hidden easter egg. Works with GNOME 45+.
